# Slide decks

PowerPoint (`.pptx`) companions to the book. Same grouping as animations and examples:

```text
ppts/
├── mathematics/
├── ai/
└── topics/
```

Decks are source files in this tree. They are not generated from LaTeX. Keep filenames in kebab-case, for example `inner-products.pptx`.
