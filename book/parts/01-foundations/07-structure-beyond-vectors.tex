\chapter{Structure Beyond Vectors}
\label{ch:beyond-vectors}

Not every object is a flat list of coordinates. Sequences, graphs, fields, and manifolds carry relations that a naive vectorisation destroys.

When the object itself has structure, the representation should respect it: permutation symmetry on graphs, locality on images, temporal order on language. The mathematical question is which invariants to preserve and which to discard.
