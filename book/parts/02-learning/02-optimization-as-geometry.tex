\chapter{Optimization as Geometry}
\label{ch:optimization}

Gradient descent is a discrete dynamical system on a loss landscape. Convexity makes local information global; without it, we only have local sensitivity.

Matrix calculus tells us how a scalar loss depends on high-dimensional parameters. The algorithm that follows from that derivative is not unique---the geometry of the parameter space still has to be chosen.
