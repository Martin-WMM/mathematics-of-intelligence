\chapter{Memory and Retrieval}
\label{ch:memory}

Parametric memory lives in weights. External memory lives in an index. Both retrieve by geometry: nearest neighbours in a representation space, or attention over a context window.

Limits of context and limits of indices are different failure modes. They should not be collapsed into one slogan about ``RAG''.
