\chapter{Modeling Distributions}
\label{ch:modeling-distributions}

A generative model states what ``plausible'' means. The statement may be a normalised density, an unnormalised energy, or a procedure that only yields samples.

Maximum likelihood is one way to fit such a model. It is not the only way, and it is not always tractable.
