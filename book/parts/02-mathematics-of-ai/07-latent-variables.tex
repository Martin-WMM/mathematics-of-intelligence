\chapter{Latent Variable Models}
\label{ch:latent-variables}

When the explicit density is intractable, we introduce latent variables and optimise a bound---typically an ELBO---via amortised inference.

The variational family is a modelling choice with the same status as the decoder. It should not be treated as a mere implementation detail.
